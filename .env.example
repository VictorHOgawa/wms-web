# URL do Adapta Hub (identidade). Sem isto, o app segue em modo demo (mock).
VITE_HUB_API_URL=http://localhost:3334
# Override opcional do spoke (quando o spokeUrl do tenant aponta p/ tunnel).
VITE_SPOKE_API_URL=http://localhost:3333
